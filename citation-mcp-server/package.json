{
  "name": "citation-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for searching and verifying citations in extracted text files",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "citation-mcp-server": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
