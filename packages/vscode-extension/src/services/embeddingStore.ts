/**
 * Re-export from core package to avoid duplication
 */
export { 
  EmbeddingStore, 
  EmbeddedSnippet, 
  EmbeddedSnippetWithSimilarity,
  QuantizedSnippet, 
  EmbeddingIndex 
} from '@research-assistant/core';
